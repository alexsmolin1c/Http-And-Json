
&НаКлиенте
Процедура ТекущиеНастройкиПользователя(Команда)
	ТекущиеНастройкиПользователяНаСервере();	
КонецПроцедуры

&НаСервере
Процедура ТекущиеНастройкиПользователяНаСервере()
	
Сообщение = Новый СообщениеПользователю;

// Создать объект записи и записать строковое значение в строку JSON.

Запись = Новый ЗаписьJSON;
Запись.УстановитьСтроку();
Запись.ЗаписатьЗначение("строковое значение");
СтрокаJSON = Запись.Закрыть();

// Показать результат.
Сообщение.Текст = СтрокаJSON;
Сообщение.Сообщить();

КонецПроцедуры

