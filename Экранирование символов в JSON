// Запись строки JSON без экранирования символов
…
Сообщение = Новый СообщениеПользователю;
// Записать строковое значение в JSON.
Запись = Новый ЗаписьJSON;
Запись.УстановитьСтроку();
Запись.ЗаписатьЗначение("путь к файлу C:/файл.txt");
СтрокаJSON = Запись.Закрыть();

// Показать результат.
Сообщение.Текст = СтрокаJSON;
Сообщение.Сообщить();
…

 Результирующая строка без экранирования слеша
"путь к файлу C:/файл.txt"

////////////////////////////////////////////////////
Запись строки JSON с экранированием символов

…
Сообщение = Новый СообщениеПользователю;

ПараметрыJSON = Новый ПараметрыЗаписиJSON( , , , , , , , ,Истина);

// Записать строковое значение в JSON.
Запись = Новый ЗаписьJSON;
Запись.УстановитьСтроку(ПараметрыJSON);
Запись.ЗаписатьЗначение("путь к файлу C:/файл.txt");
СтрокаJSON = Запись.Закрыть();

// Показать результат.
Сообщение.Текст = СтрокаJSON;
Сообщение.Сообщить();
…

 Результирующая строка с экранированием слеша
"путь к файлу C:\/файл.txt"
